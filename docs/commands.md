# Commands

The main operational commands are:

- `/abrir` — load context for a new session
- `/mapear-rotinas` — identify repeatable tasks
- `/novo-projeto` — create a dedicated project structure
- `/salvar` — save work safely and publish only when a proper remote is configured
- `/atualizar` — review and update the existing memory context
